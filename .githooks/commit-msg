#!/bin/sh
# 공개 저장소 개인정보 경계 커밋훅. [skip-record]를 포함한 어떤 메시지도 우회할 수 없다.
command -v node >/dev/null 2>&1 || exit 0
[ -f "scripts/check-public-boundary.mjs" ] || exit 0
exec node "scripts/check-public-boundary.mjs" --staged --commit-msg-file "$1"
