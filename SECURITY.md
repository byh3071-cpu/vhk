# 보안 정책

## 지원 버전

최신 `2.x` 릴리스에 보안 수정이 제공됩니다. 이전 릴리스에서 문제가 확인되면 최신 버전에서 재현 여부도 함께 알려주세요.

## 취약점 제보

공개 이슈에 토큰, 개인정보, 재현용 자격증명 또는 아직 공개되지 않은 취약점 세부 정보를 올리지 마세요. GitHub 저장소의 **Security → Report a vulnerability**에서 비공개로 다음 내용을 보내주세요.

- 영향을 받는 VHK 버전과 운영체제
- 재현에 필관리자 최소 단계
- 예상 영향과 확인한 로그(시크릿은 제거)
- 가능한 경우 완화 방법 또는 패치 제안

접수 사실을 7일 안에 회신하고, 재현 결과와 공개 일정을 협의하겠습니다. 수정이 배포되기 전까지는 조율된 공개를 부탁드립니다.

## 공개 저장소 경계

VHK는 독립 실행 가능한 공개 CLI입니다. 개인 운영 저장소, 로컬 절대경로, 개인 식별 정보와 자격증명은 공개 Git 트리와 npm 패키지에 포함하지 않습니다. `pnpm boundary:check`가 두 공개 표면을 모두 검사합니다. 사용자 규칙은 `VHK_RULES_FILE` 또는 `vhk config set-rules-file <yaml경로>`로 연결합니다.
