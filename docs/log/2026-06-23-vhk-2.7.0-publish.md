# 2026-06-23 — v2.7.0 npm 발행 (집 컴퓨터)

## 한 일
- **풀 업데이트**: 집 컴퓨터 로컬 main 이 origin/main 보다 behind 3 → `git pull --ff-only` → 로컬 v2.7.0 동기화. (노트북에서 #363 머지·태그·GitHub Release 까지 완료된 상태를 받음)
- **게이트 검증**: `pnpm build` 성공 + `pnpm test:run` **1799 pass / 177 files**(노트북과 달리 이 집 컴퓨터는 vitest 크래시 없음).
- **무결성 확인**: HEAD = origin/main = 태그 v2.7.0 커밋(`11174e5`). `v2.7.0..HEAD` 0커밋. annotated 태그라 `git rev-parse v2.7.0`이 태그객체 SHA(2957a5b)를 반환한 것 → `^{}` 미사용 시 commit SHA 와 다르게 보이는 함정 확인.
- **npm 발행**: `@byh3071/vhk` 2.6.0 → **2.7.0**. `npm view ... version` = 2.7.0, dist-tags.latest = 2.7.0 확인.

## 테스트
- 실행함: `pnpm test:run` → 1799 pass (발행 직전 + prepublish 재실행 둘 다 통과). 발행은 `--ignore-scripts`로 중복 실행 생략.

## 발행 트러블 (다음 릴리즈용 — 기억 vhk-npm-publish-webauthn.md 에도 저장)
- 계정 2FA = **Windows 보안키(WebAuthn)**, OTP(인증앱) 아님.
- Claude 의 `!` 실행은 비대화형 TTY → `ERR_PNPM_OTP_NON_INTERACTIVE`. `--otp=숫자`도 실패(보안키엔 OTP 없음).
- 해결: **별도 PowerShell 창** 직접 + `npm publish --ignore-scripts` → 브라우저 보안키 인증 → `+ @byh3071/vhk@2.7.0`.
- pnpm publish 는 보안키 웹플로우 미지원 → npm 사용(결과물 동일). npm 은 `--no-git-checks` 불필요(git 청결 검사 없음 — untracked `.vhk/*.md` 무시).

## 미완 / 다음
- 코드 변경 없음. 작업트리 = untracked `.vhk/ops-prompt.md`·`sell-prompt.md` 2개(알려진 gitignore 갭, 무해).
- 다음: `.vhk/.gitignore`+`templates/vhk-dir.ts` 보강 PR(다음부터 발행 시 `--no-git-checks` 불필요) / 또는 RFC 0056 `vhk receipt` MVP(Goal 85→86).
