# 2026-06-16 — Goal 68: vhk remind 구현

## 한 일

- `src/commands/remind.ts` — `remind()`: RULES.md 치명 섹션(NON-NEGOTIABLE/절대 규칙/Forbidden/전역 금지 다중 매칭) 불릿만 추출 → `치명: ` 접두 압축 → `.vhk/remind.md`. 후행 괄호주석(가드 #119 등) 제거. `parseRulesMd`(sync.ts) 재사용.
- 5지점 등록: index.ts(+리마인드 alias)·command-registry·nlp-router(타입+룰)·nlp-run(case)·cli-args(KNOWN_COMMAND_TOKENS+한글). ko.ts `remind.title`.
- MCP server.ts `remind` tool(runVhkCli). COMMANDS.md·README 문서화.
- `.vhk/.gitignore` + `VHK_GITIGNORE_TEMPLATE` + .vhk/README 표에 remind.md(재생성 사이드카).
- scripts/check-goal-68.mjs 고유 검증 13개(5지점 전부 강제 — goal67 #4 교훈 반영).
- tests/remind.test.ts(4): Forbidden 추출·주석/비치명 제외·절대규칙 헤더·graceful.
- mcp-cli-contract: tools 30→31 + DELEGATION remind. goals/68 status DONE + goals/README 재생성.

## 설계 결정

- **헤더 변형 흡수**: 카드는 NON-NEGOTIABLE/절대 규칙 명시하지만 실제 RULES.md는 `## VHK 운영 — Forbidden (전역 금지)` 사용 → `/non-negotiable|절대\s*규칙|forbidden|전역\s*금지/i` 다중 매칭으로 빈 산출물(쓸모 0) 회피.
- **loop-brief 형제**: 같은 사이드카 패턴(.vhk/*.md 생성 + printNextStep + graceful). 컨텍스트 재사용으로 저비용.

## 검증
- typecheck/lint ✓ · test 1713 pass(+5) · goal68 게이트 ✓
- e2e `node dist/index.js remind` → 실 RULES.md Forbidden 9불릿 추출·압축 정상(가드주석 제거 확인)

## 교훈
- 카드 스펙(헤더명)과 실제 SoT(RULES.md) 불일치 가능 → 파서는 헤더 변형 다중 매칭이 안전(단일 리터럴 매칭 시 빈 산출물).
- 4지점이 아니라 cli-args 포함 5지점 — goal67 적대리뷰 #4에서 학습. 게이트가 5지점 전부 강제.
