# 2026-06-27 — Goal 87 마감 (의도 대조 — mission ↔ receipt/review)

> append-only. 추가만, 수정·삭제 금지.

## 한 일
- **Goal 87 status: IN_PROGRESS → DONE** (`goals/87-mission-verify-intent-check.md`). `completed: 2026-06-27` 추가, Completion Check 전 항목 `[x]`.
- **`scripts/check-goal-87.mjs` 생성**: `node dist/index.js goal sync` 로 스캐폴드 백필 후, goal 고유 검증(`must()` 17건)을 채움 — 기존 `check-goal-85/86.mjs` 패턴 답습. 스텁이 아닌 채워진 게이트라야 DONE 이 `findCompletedStubGates`(meta-gate) 회귀 가드를 통과.
- **`goals/README.md` 재생성**: `node scripts/gen-goals-index.mjs` — goal 87 ✅ DONE 반영(DONE 1→2, IN_PROGRESS 4→3). 재생성 안 하면 `gen-goals-index.test.ts` 드리프트 가드 fail.

## PR3 확인 (선택 — 이미 충족)
- `src/lib/receipt.ts` `renderReceiptMarkdown` 에 `⑤ intent(의도 대조)` 행 + `mission checksum` 줄이 이미 구현돼 있음(#400 방향 3 흡수). 별도 작업 없이 충족 — 게이트 ③에 검증 박음.

## 게이트 결과
- **결정론 게이트 전부 통과**: typecheck ✓ · lint ✓ · build ✓ · secure ✓ (`vhk verify` per-gate).
- **check-goal-87.mjs 고유 검증 17건 전부 ✓** (receipt.ts intent 타입·decideReceipt forbidden→block/scope→caution·commands/receipt.ts 글루·review.ts 합류·테스트 존재).
- **영향받는 회귀 가드 직접 실행 통과**: `findCompletedStubGates`==[] · `goal drift`==clean · `goals/README` 드리프트 0 · `check-goal-frontmatter` exit 0.
- **test 게이트만 ✗ — 원인은 vitest worker crash("Worker exited unexpectedly")** = 로컬 forks/threads 풀 불안정(TS-004), 테스트 단언 실패 아님. 본 변경은 런타임 코드 0(게이트 스크립트+goal md+README)이라 테스트 회귀 불가능. goal 87 본체(PR1 #394·PR2 #406)는 이미 main 머지 = CI full suite 통과 완료. **로컬 test 빨강은 환경 탓, CI 가 진실원(TS-004)**.

## 비고
- `vhk verify` 부수효과로 `.vhk/ledger.jsonl` 저소음 커밋(evidence ledger) 1건이 worktree 에 생겼으나 PR 범위 밖 — 제외.
- `goal sync` 가 73·79 스텁도 백필했으나(누락분) 본 PR 범위 밖 → 미스테이지(BLOCKED/IN_PROGRESS 라 가드 무영향).
