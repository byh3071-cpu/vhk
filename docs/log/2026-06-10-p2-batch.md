# Dev Log — P2 무인 배치 (feat/p2-batch) · 2026-06-10

> append-only. goal별 섹션. 스펙: `~/.claude/plans/P2-RUNNER.md` + `p2-goal-*.md` + `p2-seo-22-26.md`.
> 베이스라인(시작 시): test:run **1385 pass** / build green / branch=feat/p2-batch(origin/main 동일).
> 순서: 51·52·53·54·58(독립) → 61(55+58 선행, 55 미머지면 부분) → SEO 22-26(scaffold만).

---

## Goal 51 — 출력 계층 단일화 (logger SoT + raw console 차단 가드) ✅

**스펙**: `p2-goal-51.md`. 의존 없음. 블로커 낮음.

### 한 일
- **logger SoT 승격** (`src/utils/logger.ts`): 모든 렌더를 단일 `sink` 경유(`emit`)로 라우팅.
  - `setQuiet(b)`/`isQuiet()` — 조용한 모드를 한 지점에서 토글. `error` 는 quiet 여도 노출(실패 은폐 금지, VHK 철학).
  - `setSink(fn)` — 출력 sink 교체 + **이전 sink 복원 함수 반환**(테스트 캡처/리다이렉트 격리).
  - 렌더 프린터 4종 확장: `plain`/`dim`/`bold`/`list` (기존 success/error/warn/info/step + 4).
- **신규 raw 출력 차단 가드** (`scripts/check-no-raw-output.mjs`): 선례 check-no-raw-json-parse·check-no-silent-fallback 패턴 재사용.
  - 탐지: `console.log(chalk…)` 만 (console.error/warn 제외). 다중라인 매칭(`\s` 개행 포함).
  - 예외: `// vhk-allow-raw-output: <이유>` 직전 ~160자.
  - 기본 **리포트(exit 0)**, `--strict` 시 차단(exit 1) — 기존 부채(744곳) 일괄강제 금지·점진.
  - 범위 src/**/*.ts (scripts/*.mjs 제외). logger SoT 본인은 sink 경유라 미탐.
- **점진 마이그레이션 샘플**: `src/commands/preflight.ts` 5곳 → `log.bold`/`log.plain`(chalk 합성 문자열은 sink 통과). 출력 문자열 불변.
- **테스트**: `tests/logger.test.ts`(5) + `tests/check-no-raw-output.test.ts`(6) = +11.

### TDD
- RED: 두 테스트 작성 → 9 fail(setSink 미존재·스크립트 미존재).
- GREEN: logger.ts 재작성 + check-no-raw-output.mjs 작성 → 11 pass.

### 게이트 (build먼저→tsc→test:run)
- build ✓ · tsc ✓ · test:run **1396 pass**(1385+11, 회귀 0) · check-goal-51 전 must() ✓.
- 드리프트: check-goal-51 custom must() 채움 → goals/51 `status: DONE`+`completed:2026-06-10` 전이(goal43 가드 충족, goal-drift.test 8 pass).

### 도그푸딩
- `node dist/index.js preflight` → 출력 마이그레이션 전과 동일(logger sink 경유 검증). 정상.

### 설계 결정 (ADR 불요 — 기존 패턴 답습)
- `error` 만 quiet 우회: 실패 은폐 금지 원칙. (보수 기본값)
- 가드 리포트 모드 시작: 부채 744곳, 점진 마이그레이션 압박 금지(스펙 체크포인트).

---

## Goal 52 — 테스트 사정거리 보강 (Notion 실API + restore) ✅

**스펙**: `p2-goal-52.md`. 의존 없음. 블로커 낮음. **구현부 무수정 — 테스트만 추가.**

### 한 일
- **Notion 실 API 경로**(tests/notion.test.ts 확장, `vi.mock('@notionhq/client')`):
  - mock 은 `new Client({auth})` 생성자 호환 위해 **class** 사용(arrow 는 not a constructor 실패 → 수정).
  - ① NOTION_TOKEN 누락 → 친절 에러 throw(retrieve 미호출 단언) ② fetchAllBlocks `has_more` true→false 2페이지 누적(list 2회 호출 + 두 페이지 블록 같은 섹션 병합) ③ pages.retrieve reject → 에러 전파(list 미진입).
  - beforeEach 에서 mockReset + NOTION_TOKEN 격리.
- **restore 커맨드 셸**(tests/restore.test.ts 신규): tmpdir chdir + backup.ts saveBackup 헬퍼 재사용.
  - ① id 지정 정상 복원(파일 내용 검증) ② 미존재 id → process.exitCode=1 + notFound 안내(id 포함). console.log 스파이로 출력 격리, afterEach 에서 cwd/exitCode 복원.

### TDD
- 5케이스 작성 → mock 생성자 오류(arrow) 1회 → class 로 수정 → 10 pass(notion 8 + restore 2).

### 게이트
- tsc ✓ · test:run **1400 pass**(+5, 회귀 0) · check-goal-52 전 must() ✓ · goals/52 DONE(drift 8 pass).

### 도그푸딩
- 신규 테스트가 실제 미커버 경로(auth/pagination/reject/restore exitCode) 실행 — 회귀 가드로 봉쇄. backup.test 회귀 0.

### 설계 결정 (ADR 불요)
- Client mock = class(생성자 호환). 구현부 catch 분리 불요(현 구조로 exitCode 검증 가능 — 보수적, 무수정).

---

## Goal 53 — 가드 신뢰도 (정규식 shape→behavior 이전 + CI 자동실행 기반) ✅

**스펙**: `p2-goal-53.md`. 의존 없음(46/13 모범). 블로커 낮음. **보수 스코프: 전 게이트 일괄 개조 금지.**

### 한 일
- **측정 인프라**(src/lib/goal-drift.ts): `countRegexAssertions(content)→{count,examples}`(패턴 `must(/.../.test(...))`) + `countMustAssertions`(분모). 주석·`const must=` 정의 제외.
- **메트릭 + ratchet**(tests/guard-behavior-migration.test.ts): 51개 check-goal 스캔 → 정규식 shape 비율 측정(**현재 74.6%**, 343/460), 상한 **0.85** 이하 단언 → 신규 shape 폭증 차단·점진 마이그레이션 ratchet. + behavior 테스트 목록(goal-drift/git-repo/version-sync) 존재 대조.
- **슬림 모델**(check-goal-53.mjs): shape grep 더미 대신 export 시그니처 spot check 2줄 + behavior 테스트 존재 + 비율 **동적 계산**(하드코딩 금지) + behavior 테스트 **실제 실행**. 이 게이트 자체가 shape→behavior 본보기.

### TDD
- 비율/카운트 테스트 5케이스 RED(함수 미존재) → goal-drift.ts 구현 → 5 pass.

### 게이트
- build ✓ · tsc ✓ · test:run **1405 pass**(+5, 회귀 0) · check-goal-53 ✓ · goal-drift.test 회귀 0.

### 도그푸딩
- check-goal-53 실행 → `정규식 shape 단언 343/460 (74.6%) — 상한 85%` 리포트 + 전 must() ✓. 측정 인프라 실작동 확인.

### 설계 결정 (ADR 불요 — 보수 스코프)
- **상한 0.85 (현재 ~75%)**: 내 배치가 추가하는 신규 shape 단언 흡수 헤드룸 + 폭증 차단. 마이그레이션으로 낮춰가는 ratchet.
- **CI 자동실행 = vitest 메타 테스트**로 달성(test:run=CI). 변경-goal git-diff 선택실행은 스코프 아웃(별도 후속).
- 전 게이트 일괄 개조·archive 격리는 안 함(스코프 보수). 기존 shape 단언 점진 제거 대상.

---

## Goal 54 — 제품 메타 SoT (README 버전 = package.json 동적 가드) ✅

**스펙**: `p2-goal-54.md`. 의존 없음. 블로커 낮음. **버전 하드코딩 금지(동적 비교).**

### 한 일
- **드리프트 실측·교정**: README frontmatter tag(:4) + 제목 blockquote(:9) 가 `v2.5.0` 인데 package.json 은 2.5.1 → 두 곳 v2.5.1 로 갱신.
- **가드 확장**(tests/version-sync.test.ts): 기존 CLAUDE.md 케이스 유지 + README 2케이스(frontmatter `tags:.*?v(...)` + blockquote `**v(...)**`, 상단 16줄 범위). 전부 `package.json.version` 과 **동적** 대조 — 버전 문자열 하드코딩 0.
- **SoT 선언**(tsup.config.ts 주석): package.json = 버전 SoT. 런타임 빌드 주입은 스코프 아웃(가드 정합으로 충분).
- **check-goal-54**: README 두 버전 추출 → package.json 동적 일치 + 가드 확장 확인.

### TDD
- README 2케이스 RED(v2.5.0 ≠ 2.5.1 — 드리프트 잡힘) → README 갱신 → 3 pass(CLAUDE 1 + README 2).

### 게이트
- tsc ✓ · version-sync 3 pass · test:run 1407 pass(+2, 회귀 0) · check-goal-54 ✓ · drift 0(goals/54 DONE).
- ⚠️ 1건 flaky: `cloud.gh-contract.test.ts`(실 `gh gist edit --help` 호출) — 전체 동시 실행 시 1회 실패, **격리 재실행 4/4 pass**. Goal 54 무관(README/version만 변경). 환경/동시성 flaky.

### 도그푸딩
- 가드가 실제 드리프트(v2.5.0)를 RED 로 잡고 교정 후 GREEN — version-sync README 확장이 작동함을 실증.

### 설계 결정 (ADR 불요)
- 버전 동적 비교(하드코딩 금지) — origin/main 이동 시 버전 변동 대응(스펙 명시).
- blockquote 상단 16줄 범위 검색 — 본문 다른 **굵게** 표기 오탐 방지.

---

## Goal 58 — 진화 제안 스키마 v2 (evolve-schema-v2) ✅

**스펙**: `p2-goal-58.md`. 의존 없음. 블로커 낮음. **61의 선행.**

### 한 일
- **스키마 v2**(src/commands/evolve.ts): `QUEUE_VERSION 1→2`. `TargetLayer` 5종(memory/rule/workflow/code/product) + `TARGET_LAYERS`. `EvolveQueueItem.targetLayer?`(옵션 — 하위호환) 추가, `kind:'rule'` deprecated 보존. `dedupeKey=${patternId}:${targetLayer}`(v1 'rule' 동일값).
- **마이그레이션**(migrateQueueToV2, 순수·무손실): targetLayer 기본 'rule' 부여 + dedupeKey 재계산 + version 승격. `...it` 스프레드로 전 필드 보존.
- **충돌검출**(findDedupeCollisions): 점유(pending/applied) dedupeKey 중복만 충돌, rejected 허용. 5계층 확장은 layer 다르면 키 달라 구조적 충돌 0.
- **readQueue**: `version !== 2` → 자동 변환(읽기는 디스크 미변경). 파일없음/손상 → {version:2, items:[]}.
- **writeQueue**: 쓰기 전 `.bak`(롤링) + 원본 v1 이면 `.v1.bak`(1회) → `atomicWriteFile` 원자치환 → 실패 시 `.bak` 복원.

### TDD
- 신규 13케이스 RED(migrateQueueToV2/findDedupeCollisions 미존재) → 구현 → 48 pass(기존 35 + 13).

### 게이트 (build먼저)
- build ✓ · tsc ✓ · test:run **1418 pass**(회귀 0) · check-goal-58 ✓ · check-no-raw-json-parse ✓ · drift 0.

### 도그푸딩
- `node dist/index.js evolve list` → 정상 동작(스키마 변경이 CLI 안 깨뜨림). v1→v2 무손실/백업은 테스트 13케이스로 정밀 검증.

### 설계 결정 (ADR 불요 — 하위호환 우선)
- **rename 대신 add**(`targetLayer?` + `kind` 보존): 기존 evolve.test 35케이스 무수정 통과(회귀0). version 단언 1줄만 v2 갱신.
- writeQueue 의 v1 감지용 `JSON.parse(stripBomStr(readFileSync(...)))` — stripBom 경유라 check-no-raw-json-parse 가드 통과.

---

## Goal 61 — 통계 집계 vhk stats (55 의존 → 부분) ✅(부분)

**스펙**: `p2-goal-61.md`. 의존: 55(action-ledger, **미머지**) + 58(✓). 순서상 마지막(앞).

### 한 일
- **vhk stats**(src/commands/stats.ts, 읽기 전용): 3소스 집계 — 증거 원장(PASS/WARN/FAIL) + AI 행동 원장(차단율) + 진화 큐(적용율). logger SoT 경유(Goal 51). **파일 쓰기 0건**.
- **순수 계산**: countLedgerStatus / calcBlockRate(ran=false/total) / calcApplyRate(applied/total). total 0 → rate 0(NaN 방지).
- **ai-actions reader**(src/lib/ai-actions-ledger.ts): 55 스키마(AiActionEntry 6필드) reader. 파일없음 → [] 안전 + 손상 skip + BOM-safe.
- **4지점 등록**: index.ts(.command('stats')+별칭 통계) · command-registry TOP_LEVEL · cli-args(stats/통계) · i18n ko.stats. (top-level·서브커맨드 없음 → CONTAINER 등록 불요.)
- **테스트**: stats.test(calc 3 + 읽기전용 smoke) + ai-actions-ledger.test(파일없음/파싱/손상skip/스키마/BOM) = +12.

### 블로커 (55 의존 — skip-hardstop)
- ai-actions.jsonl **생성자(runGuarded append)는 Goal 55** = 별도 배치 미머지. reader/집계/표기 완성·fixture 검증 됐으나 운영 차단율 실수치는 55 머지 후. stats 는 "데이터 없음 (action-ledger 미연동)" 정직 표기.
- `vhk blocker "[skip-hardstop] ..."` 기록(활성 0건 — HARD_STOP 미트립). **멈추지 않고 부분 완성으로 진행.**

### 함정 처리
- ai-actions-ledger.ts 주석에 `JSON.parse(readFileSync` 리터럴 → check-no-raw-json-parse **주석 오탐**(메모리 기존 사례). 주석 리워드로 해소.

### 게이트
- build ✓ · tsc ✓ · test:run **1430 pass**(회귀 0) · check-goal-61 ✓(파일쓰기0 grep) · command-registry drift 10 pass · check-no-raw-json-parse ✓.

### 도그푸딩
- `node dist/index.js stats` + `통계`(별칭) → 정상 동작, 읽기전용 출력 확인.

### 설계 결정 (ADR 불요)
- 신규 명령이지만 status.ts 시각 패턴 + Goal 51 logger SoT 둘 다 충족(log.plain(chalk…)) — raw-output 가드 신규 위반 0.

---

## SEO 22-26 — submit/check/report/automate (scaffold + 순수로직) ◐ IN_PROGRESS

**스펙**: `p2-seo-22-26.md`. ⚠️ 블로커 HIGH: 외부 API 자격증명 필요 → **무인 완성 불가**. scaffold/mock/순수로직만.

### 한 일 (무인 가능 범위 전부)
- **공유 스키마**(src/commands/seo/types.ts): `SeoLatest`(index/traffic/revenue/bing) + readSeoLatest/writeSeoLatest(원자) + mergeLatest(섹션 병합, 순수). readJsonFile(raw JSON.parse 금지).
- **22 submit**(submit.ts): IndexNow 키 생성(crypto 32hex)·검증·키파일(오프라인) + buildIndexNowPayload(순수) + ensureIndexNowKey(멱등). **구글 Indexing API 금지 가드**(상수+미호출). 실 제출=자격증명 없으면 정직 안내.
- **23·24 check**(check.ts): URL Inspection 한도 가드(2000/일·600/분, canInspect/inspectionRemaining 순수) + **AdSense v1.4 금지**·죽은 API 가드. 실 수집=자격증명 blocker.
- **25 report**(report.ts): renderSeoReportHtml(순수, 무빌드 오프라인 4블록 색인/트래픽/수익/AEO, 외부 CDN 0, ⚠️ 배지+딥링크) + --open 비대화형 스킵.
- **26 automate**(automate.ts): 확장 슬롯 인터페이스(얀덱스 자동, 다음·카카오/GBP 자리만) + sotKey(멱등, 순수) + buildSchedulerCommand(Windows schtasks, 순수). Notion 실적재=blocker.
- **4지점 등록**: index.ts(seo submit/check/report/automate) + command-registry seo:[init,submit,check,report,automate] + cli-args(seo 토큰 기존) + i18n ko.seo.{submit,check,report,automate}.

### 테스트 (mock/순수)
- seo-submit(6) + seo-check(5) + seo-report(6) + seo-automate(4) + seo-latest(5) = **+26**.

### 블로커 (실 API — 사람 개입, skip-hardstop)
- GSC/GA4/AdSense v2/Bing/Notion 실 HTTP 연동·실데이터·스케줄러 실등록 = 자격증명($VHK_SEO_*) 필요. 무인 불가 → `vhk blocker "[skip-hardstop] ..."` 기록(활성 0건).
- **DONE 아닌 IN_PROGRESS**: 실 API 미검증 scaffold라 IN_PROGRESS+blocker가 정직(goal43 드리프트는 NOT_STARTED만 잡으니 IN_PROGRESS OK). check-goal-22~26 통과(scaffold·순수·가드·등록·테스트).

### 게이트
- build ✓ · tsc ✓ · test:run **1456 pass**(+26, 회귀 0) · check-goal-22~26 ✓ · command-registry drift 통과(seo 5 서브커맨드) · drift 0.

### 도그푸딩
- `vhk seo`(라우터 5명령) · `seo submit`(사이트 미등록 안내) · `seo automate`(슬롯+스케줄러 명령) · `seo report`(latest 없음 안내) 정상.

### 설계 결정 (ADR 불요 — 스펙 충실)
- secret=$참조만(seo-config 재사용, 평문 0). 구글 Indexing API·AdSense v1.4 금지(가드+미호출). 실 연동은 운영 단계 분리.

---

## 최종 통합 — 디버깅 + /code-review high + Workflow 적대검증 + PR

### 디버깅
- 전 goal 시도 완료, revert/blocker(중단성) 0. SEO·61 의 외부의존은 정직 IN_PROGRESS/부분 + skip-hardstop 블로커.

### /code-review high (7앵글) — 발견 6건 수정(round 1)
- report.ts XSS(href javascript: 차단), evolve readQueue 미래버전 다운그레이드 방지, migrateQueueToV2 비배열 방어+죽은 삼항 제거, writeQueue 손상파일 .bak 보호, stats total 일관성, check-no-raw-output 라인주석 오탐. 잠금테스트 +4.

### Workflow 적대검증 (4차원×3-skeptic, 읽기전용, 46 에이전트) — 11건 확인 → 4건 수정(round 2)
- **수정**: ① report.ts esc(undefined/비문자열) 크래시 방어[HIGH] ② round-1 주석스킵이 `https://` 오스킵하던 회귀 정밀화 ③ mergeLatest 미래버전 다운그레이드 금지 ④ submit 손상 키파일 무경고 재생성 → 재배포 경고. 잠금테스트 +2.
- **스킵(문서화·범위밖)**: evidence-ledger/evolve 동시쓰기 lost-update(기존 아키텍처·CLI 본질, 파일락 별도 설계), findDedupeCollisions 핫패스 미연결(유틸·동작변경 범위밖), isDuplicateRule editedDraft(기존 Goal 20), calcApplyRate 분모(측정 의미 판단·자문형).
- 함정: round-2 esc 를 `String(unknown)` 으로 했더니 lint `no-base-to-string` 위반 → 전 check-goal(공유 `eslint src`) FAIL. 파라미터를 base 타입(string|number|null|undefined)으로 좁혀 lint 통과+런타임 방어 동시 달성.

### 최종 게이트
- build ✓ · tsc ✓ · **lint ✓** · **test:run 1462 pass**(베이스라인 1385 → +77, 회귀 0)
- check-goal 51·52·53·54·58·61·22·23·24·25·26 전부 ✓ · drift 0 · command-registry drift ✓ · raw-json-parse ✓.

### 산출 요약
- 완료 DONE: 51·52·53·54·58 · 부분 DONE: 61 · IN_PROGRESS(scaffold): 22·23·24·25·26.
- 커밋 10(스택), main 머지·publish·force-push 0. 블로커 2건(55 의존·SEO 실API, 둘 다 skip-hardstop·사람개입).
