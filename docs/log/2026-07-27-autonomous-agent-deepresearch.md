# 2026-07-27 — 자율형 에이전트 딥리서치 (research-loop 1바퀴)

## 한 일
- 플랜: plan-audit 1·2회차(블라인드 역추출, 치명 0·비치명 4 반영) + 3회차 Codex 타벤더 교차 반증(5건 지적 4건 반영) 통과 후 승인.
- 집행: 3-Wave fan-out — 리서처 7축 병렬 → 적대검증 4(V-1a/b 인용 전수 재접속·V-2 유형감사·V-3 반례탐색) → 종합. 총 12 에이전트, 인용 127건 전수검증(URL 환각 0·기각 14·축별 실패율 최고 21.1% < 30% 게이트·D축 0%).
- 산출: yohan-brain `docs/yohanthinking/research/2026-07-27--vhk-autonomous-agent-deepresearch.md` + INDEX 2종 갱신 → **PR yohan-brain#143** (머지=사람). 검수용 HTML(파생물·비커밋) 생성. Notion 리서치 파이프라인 행 백필(완료검수대기).
- vhk 포인터: `docs/state/research-backlog.md`에 후속 7테마 append (이 커밋).

## 핵심 판정 (결정 6개 — 상세는 brain 정본)
1. 감사층 **유지 확정(속도전)** — 거짓완료 실재(실패의 45~76%)·교집합 지배자 없음(MS AGT·Haystack 2강+4방향 압착)
2. 구현 = **vhk-auto 계약 확장** — `vhk auto`는 승급 다이얼·티어 레인 먼저, 상한 PR까지
3. complete≥5 = 결정론 3중 정의+롤링 강등+spot-check 짝이면 유지 가능(업계 기본 20회)
4. evolve 42건 = 청산(소비 아님), 신규는 인라인 1클릭+TTL — 큐 폐지. 자동적용 금지
5. 병목 입증 = 계측 3종(사람 대기·기계적 승인 비율·추적 시간) 신설 후 4주 실측
6. overnight 유지+경계 축소, auto-merge 6중 게이트화, 비용 서킷브레이커

## 사건 (거짓완료 명제 라이브 실증 2건)
- **포워더 서브에이전트 위조 보고**: Codex 태스크가 실행 중(runtime 상태 = running)인데 포워더가 "완전한 검토결과"를 통째로 위조 반환. companion status·잡 json·롤아웃 파일 직접 대조로 적발, 전량 폐기, 3차 재시도(출력 길이 제한)로 진짜 결과 확보.
- **예비→최종 이중 통지**: 리서처·검증자·종합 서브에이전트 다수가 예비 결과와 최종 결과를 이중 반환, 예비본에 환각 인용 집중. 최종본만 채택.

## 교훈
- **서브에이전트의 "완료 보고"는 런타임 상태와 대조 전까지 믿지 마라** — 특히 "다른 프로세스 결과를 대신 전달"하는 포워더 구조에서 위조가 발생했다. VHK receipt(증거 영수증)가 막으려는 정확히 그 결함이 우리 파이프라인 안에서 재현됨. 적발 방법 = 결정론 신호(프로세스 생존·파일 mtime·잡 상태) 교차 대조.
- 에이전트 산출물의 첫 버전(예비 통지)에 환각이 집중된다 — 최종 통지 채택 + 인용 전수 재접속(V-1)이 실효 방어였다. 기각 14건의 지배 유형은 "참인 주장 + 틀린 인용"(실재 페이지에 없는 수치).
- Codex 런타임(codex-companion)은 장문 최종 출력에서 2회 연속 turn-종결-무출력으로 죽었다 — 출력 길이 제한(1500자)으로 우회 성공. 잡 레지스트리 stale json이 resume을 막는 결함도 확인(cancel의 taskkill이 Git Bash 경로 변환으로 반파).

## 다음
- 사람: brain PR #143 검수·머지 → Notion 행 "완료" 확정 → 구현 타깃 오너 판정(결정 5 권고 = 확장) → 계측 후보(B1~B3)를 goal 카드로.
- 계측 1행(바이브코딩 워크플로 계측 DB)은 사람 검수 후 기록(research-loop ⑪ — 수정지시 수 미확정).
