# 회고 — v0.6~v1.0: MCP 도입 → GA (2026-05-24, 역시 하루)

> governance T5 백필 회고(2026-06-11 작성). git 태그·CHANGELOG 기반 재구성.

## 무엇이 만들어졌나

- v0.6.0(12:19) **MCP 8 tools** + mcp-init + execFileSync 보안화([ADR-002](../adr/ADR-002-mcp-architecture.md)).
- v0.7.x deploy/env/publish + safeExecFile 공유 → v0.8.0(15:24) **design/theme/ref**
  ([ADR-003](../adr/ADR-003-design-theme-ref.md)) → v0.9.0 harness/audit/migrate/update.
- v1.0.0(18:29) **GA 선언** — context/memory/brief + API 안정성 약속.

## 배운 것

- v0.6 의 "CLI 를 감싸는 MCP" 선택이 이후 전부를 결정 — 29 tools 까지 재작성 0회.
  초기 아키텍처 결정의 레버리지가 가장 크다는 실증.
- GA 를 하루 만에 선언한 대가: "기존 시그니처 변경 금지"가 그날 이후 모든 리팩터의
  제약이 됨. 약속은 싸고 유지는 비싸다 — 단 이 제약이 단일 SoT(goal 48) 같은
  비파괴 리팩터 기법을 강제로 발전시킴.

## 손실 구간 (정직 표기)

- v0.6~v1.0 사이 6시간의 의사결정 토론 기록 없음. MCP stdio vs HTTP, GA 타이밍 판단
  근거는 추정만 가능(ADR-002 에 추정 표기).
