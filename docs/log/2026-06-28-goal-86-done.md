# 2026-06-28 — Goal 86 마감 (vhk receipt MVP — 4대 기계증거 영수증, RFC 0056 T1)

> append-only. 추가만, 수정·삭제 금지.

## 한 일
- **Goal 86 status: IN_PROGRESS → DONE** (`goals/86-receipt-mvp.md`). `completed: 2026-06-28` 추가(goal 87 컨벤션 답습). 코드 본체는 이미 main 머지(#377)·정정(#380) — 이번엔 **수용 기준 실측 검증 후 상태 전환만**.
- **`goals/README.md` 재생성**: `node scripts/gen-goals-index.mjs` — goal 86 ✅ DONE 반영(DONE 2→3, IN_PROGRESS 3→2). 재생성 안 하면 `gen-goals-index.test.ts` 드리프트 가드 fail.

## 수용 기준 실측 (DONE 근거 — 4/4 충족)
1. **등록 4지점 + nlp + 한글별칭 + 드리프트 테스트** ✓ — `src/index.ts:609-610`(`.command('receipt')`+`.alias('증거영수증')`) · `command-registry.ts:100`(TOP_LEVEL leaf) · `cli-args.ts:67`(KNOWN_COMMAND_TOKENS) · `ko.ts:291`(receipt 네임스페이스) · `nlp-router.ts:162-165`(라우팅+키워드). 봉인: `tests/receipt-registration.test.ts`(7 케이스).
2. **단조성 불변식(caution→pass 금지)** ✓ — `receipt.ts:100-114` 폭포(block?→caution?→pass, 역행 분기 0) · `tests/receipt.test.ts:76-111`.
3. **diff-cover advisory 분리(block 격하 불가)** ✓ — block 분기(`receipt.ts:105`)에 diffCover 없음(red·dirty·stale·forbidden만) · `tests/receipt.test.ts:113-134`(0% 커버→caution).
4. **`.md` 정직성 1줄** ✓ — `HONESTY_LINE`(`receipt.ts:85-86`, "게으른 거짓완료…미묘한 오류는 못 잡는다") `renderReceiptMarkdown:290` 출력 · 테스트 `tests/receipt.test.ts:165-167`.

## 게이트 결과
- **`pnpm build` ✓** (tsup ESM+DTS success).
- **`check-goal-86.mjs` ✓** — typecheck ✓ · lint ✓ · goal 고유 검증 21건 전부 ✓(순수핵심 분리·HONESTY_LINE·실차단 3종만 block·등록 4지점·불변식 테스트 존재).
- **`tests/receipt-registration.test.ts` 7/7 ✓** (`--pool=threads`, exit 0).
- **순수 불변식 15건 ✓** — tsx 단독 스크립트로 `decideReceipt`/`renderReceiptMarkdown` 직접 검증(clean→pass·red/dirty/stale→block·soft/uncovered/stale미상→caution·0%커버→caution아닌block·dirty+풀커버→여전히block·HONESTY_LINE 2구절). 임시파일 검증 후 삭제.
- **`tests/receipt.test.ts` 풀파일 vitest ✗ — vitest worker crash(exit 127 / "Worker exited unexpectedly")** = 로컬 forks/threads 풀 불안정(TS-004) + IO 무거운 임시 git 레포 테스트의 rmSync 환경이슈(TS-005). 단언 실패 아님. 본 변경은 런타임 코드 0(goal md+README+dev log) — 회귀 불가능. 코드 본체는 #377 로 이미 CI full suite 통과. **로컬 test 빨강은 환경 탓, CI 가 진실원(TS-004)**. 순수 로직은 위 tsx 단독으로 green 확인.

## 비고
- **수용기준 3 미세 차이(차단 본 게이트는 이제 4조건)**: goal 87(#406, 이미 DONE)이 `forbiddenViolated`(mission 금지 glob 매치 = 결정론)를 block 분기에 추가 — goal 86 당시 "실차단 3종"의 합법적 확장. **load-bearing 요건("diff-cover는 advisory, block 격하 불가")은 그대로 유지** → DONE 불방해. 수용기준 텍스트는 당시 기록이라 **수정 안 함**(사후 짜맞춤 금지).
- 변경 파일 = `goals/86-receipt-mvp.md` · `goals/README.md` · 본 dev log. src/** 무변경.
