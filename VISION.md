# VISION — vhk

> 용어: ADR-011 대응표 참조.
> 북극성 앵커. 루프가 매 틱 의도 재고정용으로 읽는다. 짧게 유지.
> 자주 바뀌는 상태는 .vhk/context.md 로. 여기엔 변하지 않는 의도만.

## What (한 줄)
모델·에이전트를 뭘로 바꿔도 안 무너지는 풀사이클 AI 코딩 하네스. (README·README.en 헤드라인과 동일)

- **풀사이클** = 코딩 앞뒤까지. 기획·규칙 수립부터 검증·회고·운영 산출물까지 한 루프.
- **핵심 기능의 정체성 문구**(ADR-011): "AI 코딩 결과를 테스트·빌드 결과로 대조하는
  1인 개발자용 검증 도구." 하네스 전체가 아니라 `verify`·`review`·`receipt` 축을 가리킨다.
  "탐지기" 같은 단정형은 적발 사례가 쌓인 뒤에 되돌린다(ADR-011 §정체성 문구).

## Why (북극성)
한국어 개발자가 AI 에이전트와 협업할 때 목표 표류·증거 부재·규칙 증발을 막는다.
vhk가 없으면 LLM은 매 세션 방향을 잃는다. vhk가 있으면 100번째 턴도 1번째와 같은 의도를 유지한다.

## 도구 교체 내성 (설계 전제)
어떤 모델·에이전트를 얹어도 안 무너져야 한다. 규칙·기억·게이트는 에이전트가 아니라
**저장소 안의 파일**에 남기고, 판정은 종료코드 같은 기계 증거로 한다.
특정 벤더의 기능에 의존하는 설계는 이 전제를 깬다.

## 상위 아키텍처 의도 (Proposed — ADR-012)

> **에이전트는 바뀌어도, 시스템은 남고 검증을 통해 진화한다.**

남아야 하는 것은 의도·프로젝트 상태·작업·정책·결정·검증 증거·학습과 롤백 이력이다.
모델·IDE·오케스트레이터는 교체 가능한 실행 표면으로 둔다. 범용 VHK Core는 계약과 검증만
소유하고, 개인 지식·관제·캘린더 연결은 외부 통합 레이어가 Core에 의존한다. 반대 의존은 금지한다.

이 문단은 공개 기능 완료 주장이 아니라 [ADR-012](docs/adr/ADR-012-agent-agnostic-core-and-method-absorption.md)의
승인 대기 아키텍처 방향이다. 공개 정체성 문구와 현재 Definition of Done은 그대로 유지하며,
교차 에이전트·자가개선 주장은 실측 증거가 생긴 범위에서만 사용한다.

## Definition of Done (v1 출시 기준)
- [x] `vhk init` 한 번으로 목표·규칙·컨텍스트·기억 구조 생성
- [x] `vhk check` 로 게이트 통과 증거 자동 수집
- [x] `vhk memory` 로 교훈·결정·실패·성공 4버킷 관리
- [x] `pnpm publish` 이전 모든 게이트(typecheck·lint·test·build) 통과

## Non-goals (범위 수비)
- UI/대시보드 — CLI 전용, 시각화는 Notion·MCP 위임
- 다국어 지원 — **한국어 우선.** CLI 출력·메시지는 `ko.ts` 단일소스로 한국어만 유지하고,
  영어는 외부 유입용 문서(`README.en.md`) 한 장까지만 둔다. 출력 i18n 은 범위 밖
- LLM 직접 호출 — vhk는 구조 도구, 모델 선택은 사용자 몫

## Loop Anchor (루프가 매 틱 지킬 것)
- 한 번에 goal 1개. STOP 조건 우선.
- 의심되면 멈추고 사람 확인.
- .vhk/HARD_STOP 있으면 즉시 중단.
